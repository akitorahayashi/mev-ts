---
# Master list of AGENTS.md sections. Declared order is the concatenation order,
# and the listed set is the authority for which sections exist. Each entry maps
# to a sibling `<name>.md`. A listed name without a `.md`, or a `.md` without a
# listing, is a startup error.
sections:
  - communication
  - rtk
  - coding
  - documentation
  - testing
