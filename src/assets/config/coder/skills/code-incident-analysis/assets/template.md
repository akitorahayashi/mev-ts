# <調査対象>

## 結論

<最初の一文でクラッシュ経路が「コード上で再現する」「コード上で再現しない」「コード上では判定不能」のどれかを示す。原因を一文で説明し、本番で入力が生まれた理由や通常操作の再現可否に未確定事項があれば分けて示す。>

## 原因

<正常時と失敗時が最初に分かれる値または状態だけを、技術用語の意味とともに説明する。比較が理解を早める場合だけ小さな表を使う。判定に不要な入力や可能性を列挙せず、根拠コードは主張の直後に置く。>

## 発生順序

1. <画面または処理の開始地点を示す。>
2. <失敗する処理へ値が渡る経路を示す。>
3. <値が不正になる処理を示す。>
4. <その値を使用して例外が発生する処理を示す。>

<非同期処理が失敗経路を変える場合だけ、その完了後にどの手順へ戻るかを示す。各手順にはエディタで順に辿れるコードリンクを置く。>

## 再現手順

<通常のユーザー操作で再現できる場合は、その操作を番号付きで示す。コードやデバッガーによる状態作成が必要な場合は「コード上の最小再現手順」と明記し、ユーザー操作として扱わない。通常操作の手順が未確定なら、その事実と確認に必要な最小限の情報を示す。>

## 根本対応

<修正する境界、正常な入力の扱い、不正な入力の扱いを一つの対応として決め、失敗を防げる理由を示す。同じ不正な処理を持つ実装が確認できた場合だけ、同じ修正が必要な影響範囲として示す。代替案や任意の改善案は並べない。>
